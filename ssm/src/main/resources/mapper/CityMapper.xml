<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.google.ssm.dao.CityDao">
	<resultMap id="BaseResultMap" type="com.google.ssm.domain.City">
		<result column="id" property="id" />
		<result column="province_id" property="provinceId" />
		<result column="city_name" property="cityName" />
		<result column="description" property="description" />
	</resultMap>

	<!--<parameterMap id="City" type="com.google.ssm.domain.City"/>-->

	<sql id="Base_Column_List">
		id, province_id, city_name, description
	</sql>

	<select id="findByName" resultMap="BaseResultMap" parameterType="java.lang.String">
		select
		*
		from city
		where city_name = #{cityName}
	</select>

	<!--<select id="getAllM" resultType="OrderDetail">-->
		<!--select * from tb_order_detail-->
	<!--</select>-->

	<insert id="mybatisInsert">
		INSERT INTO `test`.`student` (`name`, `age`)
		VALUES
			('周杰伦', '11');
	</insert>

</mapper>
